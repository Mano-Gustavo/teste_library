💎 MANO GUSTAVO UI FRAMEWORK
Diamond Edition — V8.0 (Hybrid Hub Framework)

Uma library moderna, modular e profissional para criação de scripts simples e Hubs multi-jogos, com suporte a Key System, Game Hub, Configs, Temas e Componentes avançados.

⚠️ Esta library é HÍBRIDA
Ela pode ser usada tanto para scripts simples quanto para Hubs completos.
Nada é obrigatório: você usa apenas o que quiser.

📥 [1] INSTALAÇÃO (LOADER VIA GITHUB)

A library é carregada remotamente, evitando scripts gigantes.

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Mano-Gustavo/Mano-Gustavo-Library/refs/heads/main/library.lua"))()


✅ Vantagens:

Script final fica muito menor

Atualizações automáticas

Organização profissional

🔐 [2] SISTEMA DE KEY (OPCIONAL)

Usado apenas se você quiser proteger o script (Hubs pagos ou privados).

⚠️ Deve ser chamado ANTES de CreateWindow

Library:InitKeySystem({
    Title = "Titanium Key System",
    Keys = {"key123", "admin2024"},
    Discord = "https://discord.gg/seuservidor",
    Info = "Join our Discord to get a key!",
    OnSuccess = function()
        print("Access Granted!")
    end
})


📌 Importante:

É uma proteção visual (client-side)

Para segurança real, use whitelist em servidor externo

🪟 [3] CRIANDO A JANELA PRINCIPAL
local Window = Library:CreateWindow({
    Title = "Mano Gustavo Hub",
    Keybind = Enum.KeyCode.RightControl
})

Recursos da Janela:

Drag & Drop

Toggle de visibilidade

Suporte Mobile (botão flutuante automático)

Tabs laterais

🧠 [4] DOIS MODOS DE USO (IMPORTANTE)
🔹 MODO 1 — SCRIPT SIMPLES

Use como uma UI normal (igual V7.9).

local Tab = Window:CreateTab("Farm")
Tab:CreateToggle("Auto Farm", function(v) end)


✔ Sem Key System
✔ Sem Game Hub
✔ Menu direto

🔹 MODO 2 — HUB PROFISSIONAL (NOVO)

Usado para scripts multi-jogos.

local Hub = Window:CreateGameHub("Game Hub")


Esse modo cria:

Lista de jogos

Detecção automática por PlaceId

Lista de scripts por jogo

🎮 [5] SISTEMA DE GAME HUB (HUB MULTI-JOGOS)
local Hub = Window:CreateGameHub("Supported Games")

Hub:AddGame("Blox Fruits", {
    AutoLoad = true,
    PlaceIds = {2753915549, 4442272183},
    Scripts = {
        {
            Name = "Auto Farm",
            Desc = "Automatic farming system",
            Callback = function()
                loadstring(game:HttpGet("LINK"))()
            end
        }
    }
})

Vantagens:

Organização extrema

UX profissional

Padrão usado em Hubs pagos

Evita menus gigantes

🗂️ [6] ORGANIZAÇÃO: TABS E SECTIONS
Criar Aba (Tab)
local Tab = Window:CreateTab("Combat")

Criar Seção (Group / Section)
local Section = Tab:CreateSection("Aimbot Settings")


📌 Tudo criado usando a Section ficará dentro da caixa.

🎛️ [7] COMPONENTES SUPORTADOS
🔘 Button
local Btn = Section:CreateButton("Reset", function() end)
Btn:SetTooltip("Resets your character")

🔁 Toggle (com Hooks)
local Toggle = Section:CreateToggle("Auto Farm", function(v) end)

Toggle:OnEnable(function() end)
Toggle:OnDisable(function() end)

🎚️ Slider
local Slider = Section:CreateSlider("FOV", 50, 500, 100, function(v) end)


📌 Importante:
O callback é responsável por sincronizar sua lógica (ex: tamanho do círculo).

📋 Dropdown
Section:CreateDropdown("Weapon", {"Sword", "Gun"}, function(v) end)

🎨 Color Picker (RGB)
Section:CreateColorPicker("ESP Color", Color3.fromRGB(255,0,0), function(c) end)

📝 TextBox
Section:CreateTextBox("Message", "Type here", function(t) end)

🏷️ Label
Section:CreateLabel("⚠ Dangerous Area")

💬 [8] TOOLTIP SYSTEM

Disponível para todos os componentes.

Element:SetTooltip("This does something important")


Aparece ao passar o mouse

Ignorado automaticamente no mobile

💾 [9] SISTEMA DE CONFIG (SAVE / LOAD)

Salva automaticamente:

Toggles

Sliders

Dropdowns

Color Pickers

Library:SaveConfig("Legit")
Library:LoadConfig("Legit")
Library:ResetConfig()


📌 Observações importantes:

As flags usam o nome do componente

Evite nomes duplicados

Sistema é client-side (JSON)

📂 Caminho:

workspace/GustavoHub/Configs/

🎨 [10] SISTEMA DE TEMAS (DYNAMIC THEME)
Library:SetTheme({
    MainColor = Color3.fromRGB(20,20,20),
    SecondaryColor = Color3.fromRGB(35,35,35),
    AccentColor = Color3.fromRGB(255,0,0),
    TextColor = Color3.fromRGB(255,255,255),
    SectionColor = Color3.fromRGB(30,30,30)
})


✔ Atualiza tudo em tempo real
✔ Usa Tween suave
✔ Sistema de registro interno (ThemeRegistry)

🔔 [11] NOTIFICAÇÕES
Library:Notification({
    Title = "Success",
    Text = "Config Loaded",
    Duration = 5,
    Type = "Success"
})


Tipos:

Success

Error

Warning

Info

🧪 [12] BOAS PRÁTICAS

✔ Use Sections
✔ Evite nomes duplicados
✔ Centralize lógica fora da UI
✔ Use callbacks para sincronização
✔ GameHub só quando necessário

🏁 CONCLUSÃO

Esta library não é mais apenas uma UI.
Ela é um FRAMEWORK COMPLETO, capaz de:

Scripts simples

Hubs profissionais

Monetização

Organização extrema

Expansão futura
